# 项目计划：Clarifydeck

## 1. 系统架构与数据真实源 (SSOT)
- 后端 (Python) 作为全局状态的唯一真实源，维护一个结构为 `dict[box_id, BoxState]` 的内存数据字典。
- 前端 (React) 仅作为视图展现层与参数发送终端，不本地持久化存储框体坐标数据。

## 2. 后端核心功能 (main.py)
- **单例截图驱动**：后端启动一个全局非阻塞异步循环，每 0.5 秒调用一次系统截图，获取整帧画面并暂存于内存中。
- **内存分片与比对**：遍历当前已存在的矩形框坐标，在内存中对整帧画面进行 crop 切片。对每个切片执行轻量级图像差异比对（如 dHash 或 MSE），若差异低于阈值则跳过该框的本次 OCR。
- **受控 OCR 调用**：若某切片图像发生显著变化，检查 `/home/deck/Clarifydeck/bin/tesseract` 是否存在。若存在，在注入 `TESSDATA_PREFIX` 环境变量的前提下发起命令行调用；剥离结果中的非法换行与乱码符号后，通过 `decky.emit("ocr_broadcast", { "id": str, "text": str })` 异步推送至前端。

## 3. 前端核心功能 (src/index.tsx)
- **快捷菜单面板 (QAM)**：
  - 提供 `+` 按键：调用后端 RPC，在屏幕中心点新增一个默认大小（300x60）的文本框数据实例，并自动将其设为当前选中项。
  - 提供列表组件：罗列当前所有的文本框实例，支持点击切换“当前选中文本框”。
  - 提供 4 个坐标调整滑块（X, Y, W, H）：拖动时实时修改当前选中框的坐标参数，并同步更新后端内存字典。
  - 提供 `-` 按键：调用后端 RPC，销毁当前被选中的文本框实例。
- **全屏游戏穿透层 (Overlay)**：
  - 在屏幕最顶层挂载一个 `pointer-events: none` 的全屏透明固定容器。
  - 监听后端的 `ocr_broadcast` 事件，根据各文本框的坐标，在该容器的对应物理位置上实时渲染黑底白字、带毛玻璃效果的独立半透明字幕框。